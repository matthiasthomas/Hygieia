(function (window) {
  window.__env = window.__env || {};

  // Dev GTA Tag ID
  window.__env.analyticsTagID = 'UA-116158158-1';

  // Setting this to false will disable console output
  window.__env.enableDebug = false;
}(this));